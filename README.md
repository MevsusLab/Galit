# ГАЛИТ

**Поддержка решений для осложнённого фонда.**
Галит, кальцит, АСПО и CO₂-коррозия — screening на одной шкале риска с trust gate.

Прототип для конкурса «Марафон ИТ-стартапов – 2026» (РУП «ПО «Белоруснефть»).

- Текст заявки — [ЗАЯВКА.md](ЗАЯВКА.md)
- Анализ конкурса и банк из 11 идей — [PITCH.md](PITCH.md)

---

## Содержание

1. [Что это такое](#1-что-это-такое)
2. [Зачем это нужно](#2-зачем-это-нужно)
3. [Как это работает](#3-как-это-работает)
4. [Установка](#4-установка)
5. [Быстрый старт](#5-быстрый-старт)
6. [**Какие данные нужны**](#6-какие-данные-нужны) ← главное
7. [Как читать результат](#7-как-читать-результат)
8. [Разбор фонда скважин](#8-разбор-фонда-скважин)
9. [Экономика](#9-экономика)
10. [Структура кода](#10-структура-кода)
11. [Границы применимости](#11-границы-применимости--читать-обязательно)
12. [Типичные ошибки](#12-типичные-ошибки-при-использовании)
13. [Источники моделей](#13-источники-моделей)
14. [Baseline-сравнение и измеримый пилот](#14-baseline-сравнение-и-измеримый-пилот)

---

## 1. Что это такое

> **Одной фразой:** ГАЛИТ заранее показывает, какая скважина находится под угрозой отложений или коррозии, где именно возникнет осложнение и какое профилактическое действие следует выполнить до снижения добычи, отказа оборудования или дорогостоящего ремонта.

ГАЛИТ — инженерная система раннего предупреждения и поддержки решений для добывающего фонда РУП «ПО «Белоруснефть». По стандартным промысловым данным
одной скважины (химанализ воды, дебиты, конструкция, геотермия) она считает:

- **где именно** в стволе начнутся отложения парафина — глубину в метрах;
- **выпадет ли соль**, и если да — сколько килограммов на кубометр воды;
- **с какой скоростью** корродирует НКТ, в мм/год;
- **какой механизм главный** на этой конкретной скважине;
- **что делать** — конкретную технологию из арсенала НГДУ;
- **чего при этом нельзя делать** — конфликты между технологиями.

А по фонду скважин — **ранжированную очередь обработок**: кого первым.

Это чистый Python, ядро расчёта работает **без внешних зависимостей**.

---

## 2. Зачем это нужно

### Проблема

Скважина Припятского прогиба одновременно подвержена **четырём**
механизмам осложнений:

| механизм | природа | чем управляют |
|---|---|---|
| **галит** (NaCl) | охлаждение рассола 1,2 г/см³ при подъёме | промывки пресной водой |
| **кальцит** (CaCO₃) | сброс давления → дегазация CO₂ → рост pH | ингибиторы солеотложения |
| **АСПО** | остывание потока ниже температуры насыщения парафином | СГБ, КР-01, скребки, греющий кабель |
| **CO₂-коррозия** | углекислотная коррозия стали | ингибиторная защита |

Каждым занимаются отдельно. Но:

**Они конкурируют за одну операцию.** Один выезд бригады, один простой
скважины, один бюджет. Четыре отдельные модели дают четыре несравнимых
«риска 0,7» — по ним нельзя отсортировать фонд.

**Они физически взаимодействуют:**

```
отложения сужают сечение НКТ
        ↓
растёт скорость потока
        ↓
растёт массоперенос
        ↓
УСКОРЯЕТСЯ КОРРОЗИЯ
```

На versioned synthetic/scenario illustration из ранней версии демо: 0,291 → 0,399 мм/год. **Рост на 37 %**,
который односвязная модель не увидит в принципе.

**Технологии конфликтуют:**

- скребок снимает АСПО, но **разрушает защитную плёнку FeCO₃** →
  коррозия ускоряется;
- кислотная обработка от карбонатов **усиливает** коррозию.

Пока четыре механизма считаются в четырёх разных таблицах, эти связи
не видит никто, и мероприятия назначаются **по графику, а не по риску**.

### Что даёт ГАЛИТ

Общую шкалу → можно ранжировать. Учёт взаимодействия → прогноз коррозии
не занижен. Проверку конфликтов → не назначить взаимоисключающие
мероприятия.

### Ценность для «Белоруснефти»

ГАЛИТ помогает перейти от реакции на уже случившуюся проблему к **предупредительному обслуживанию**. Вместо одинаковых обработок по календарю предприятие получает обоснованную очередь скважин: где риск выше, какое осложнение ожидается, на какой глубине и какое мероприятие целесообразно выполнить первым.

Практический эффект системы:

- снижение вероятности закупорки НКТ, повреждения труб и аварийных остановок;
- сокращение потерь добычи и внеплановых ремонтов;
- более точное планирование промывок, скребкования и ингибиторной защиты;
- рациональный расход реагентов и рабочего времени бригад;
- единая прозрачная картина риска для технологов, геологов и руководителей;
- сохранение инженерных знаний в воспроизводимой цифровой модели, а не в разрозненных таблицах.

ГАЛИТ не заменяет инженера — он превращает промысловые данные в понятное и проверяемое решение: **какую скважину обслужить первой, почему именно её и что произойдёт, если отложить обработку**.

> **Целевой результат:** предупредить осложнение до того, как оно превратится в падение дебита, простой или дорогостоящий ремонт.

---

## 3. Как это работает

Расчёт по одной скважине идёт в 6 шагов:

```
1. ПРОФИЛИ В СТВОЛЕ
   T(z) — модель Ramey (1962), аналитическое решение
   P(z) — Beggs & Brill (1973), маршевый расчёт снизу вверх
                    ↓
2. АСПО
   считаем WAT(P) в каждой точке
   ищем ПЕРЕСЕЧЕНИЕ кривой T(z) и кривой WAT(P(z))
   → это и есть глубина начала отложений
                    ↓
3. СОЛИ (в самой холодной точке — на устье)
   галит:   SI = log10( a(Na⁺)·a(Cl⁻) / Ksp ),  Питцер
   кальцит: Stiff-Davis (с проверкой границ применимости)
                    ↓
4. КОРРОЗИЯ (в самой горячей точке)
   de Waard-Milliams, resistance model 1995
   1/V = 1/V_реакция + 1/V_массоперенос
                    ↓
5. ВЗАИМОДЕЙСТВИЕ  ← вот ради этого всё
   если отложения сильные → сужаем сечение
   → пересчитываем скорость → ПЕРЕСЧИТЫВАЕМ КОРРОЗИЮ
                    ↓
6. ИНТЕГРАЛЬНЫЙ РИСК
   взвешенная сумма четырёх механизмов
   → доминирующий механизм → подбор технологии → проверка конфликтов
```

### Почему пересечение кривых, а не машинное обучение

Мы предсказываем не только **факт** осложнения, но и **глубину**.
А глубина определяет выбор технологии:

- скребок достаёт не везде (для ЭЦН — примерно до 1500 м);
- греющий кабель имеет ограниченную длину;
- растворитель закачивается на **конкретную отметку**.

Градиентный бустинг сказал бы «риск 0,8» и не сказал бы, куда лить
реагент. Плюс физическая модель работает на скважине, по которой нет
истории отказов, — а именно там она нужнее всего.

---

## 4. Установка

Нужен **Python 3.10+** (используются `X | Y` в аннотациях типов).

Расчётное ядро `galit/` использует только стандартную библиотеку. Интерфейсы и XLSX/графики ставятся из зафиксированных совместимых зависимостей:

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
# для разработки и тестов:
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

Проверка окружения:

```powershell
.\run_demo.ps1
# Запускает полный pytest, формирует evidence manifest и выполняет demo.
# Актуальное число тестов записывается в reports/release-manifest.{json,md}.

.\.venv\Scripts\python.exe -m compileall -q galit api.py dashboard.py telegram_bot.py calibration_cli.py release_manifest.py
```

> На Windows перед запуском полезно выставить
> `$env:PYTHONIOENCODING="utf-8"` — иначе консоль в cp1251 споткнётся
> на кириллице в выводе.

---

## 5. Быстрый старт

### Вариант А: посмотреть демонстрацию

```bash
python demo.py              # полный расчёт в консоль
python demo.py --plots      # + графики в galit_demo.png
```

Демо покажет 4 раздела: границы применимости индексов, разбор одной
скважины, ранжирование фонда из 40 скважин, экономику с чувствительностью.

> **Данные в демо синтетические.** Физика настоящая, фонд смоделирован
> по опубликованным характеристикам Припятского прогиба. Это иллюстрация
> работы пайплайна, а не прогноз по чьему-то реальному фонду.

### Вариант Б: посчитать свою скважину

```bash
python example_one_well.py
```

Откройте [`example_one_well.py`](example_one_well.py), подставьте свои
цифры — там каждое поле подписано с единицами измерения.

### Вариант В: интеграционный API-прототип

```powershell
.\run_api.ps1
```

OpenAPI: `http://127.0.0.1:8000/docs`. Пример входа —
`examples/sample-well.json`, локальная Postman collection —
`postman/collections/GALIT API/`.

Контракты: `GET /api/v1/health`, `GET /api/v1/readiness`,
`POST /api/v1/diagnose`, `POST /api/v1/diagnose/bulk`,
`POST /api/v1/master-plan`. Bulk и master-plan ограничены
`GALIT_MAX_BATCH_SIZE` (по умолчанию 25) и работают по политике partial
success: валидные строки рассчитываются, ошибки остальных возвращаются
поэлементно; превышение лимита — 413, пустой/невалидный envelope — 422.
Расширенная metadata в single включается `include_metadata=true`, подробные
профили — только `include_profiles=true`. Каждый ответ содержит `X-Request-ID`.

CORS по умолчанию запрещён; разрешённые origin задаются явным списком
`GALIT_CORS_ORIGINS=https://ui.example`. Wildcard запрещён. API не логирует
payload, токены и тела запросов. Аутентификация/авторизация не реализованы и
остаются roadmap — этот этап не делает заявлений production-ready.

Однокомандный контейнерный запуск:

```bash
docker build -t galit-api .
docker run --rm -p 8000:8000 galit-api
```

Контейнер работает non-root и имеет healthcheck; `.dockerignore` исключает
`.env`, сырые `data`, `reports` и calibration artifacts. Если Docker недоступен,
используйте `python -m compileall`, pytest и статические тесты упаковки.

### План мастера на сегодня

Функция превращает результаты диагностики фонда в воспроизводимую очередь
задач. Сначала исключаются строки ниже `min_risk`, затем дубли скважин
(без учёта регистра и повторных пробелов) сводятся к результату с наибольшим
риском, после чего задачи сортируются по классу срочности, убыванию риска и
имени. `limit` применяется только после сортировки; `0` означает пустой план.
Пороги: `>=0.75` — немедленно, `>=0.60` — 24 ч, `>=0.45` — 48 ч,
`>=0.35` — 72 ч, ниже — планово.

`possible_oil_loss.lower_m3d/central_m3d/upper_m3d` — **screening estimate**
добычи под риском: дебит нефти умножается на сценарные p05/p50/p95 risk score,
а без uncertainty используется диапазон 0.5×/1×/1.5× score с ограничением
0…1. Это не вероятность отказа и не прогноз фактической потери; без полевой
калибровки показатель годится только для сравнительного ранжирования.

Основные поля задачи: `priority`, `level`, `risk`, `dominant`, `response_deadline`,
`recommended_action`, `pre_trip_checklist`, `materials`, `equipment`,
`quality_warnings`, `production_ready`, `safe_to_act`. Если quality gate не
пройден, `safe_to_act=false`, а воздействие явно блокируется. Даже при
`safe_to_act=true` обязательны проверка фактических данных, HSE-оценка,
совместимость жидкостей/реагентов, наряд-допуск, утверждённая технологическая
карта и решение ответственного лица; функция не назначает дозировки.

**Dashboard:** светлый адаптивный shell сохраняет шесть рабочих разделов и
добавляет обзор фонда перед ними: четыре KPI, текущий риск по скважинам, top-5,
donut доминирующих осложнений и правую ленту alerts. Навигация продублирована
компактным списком в боковой панели; upload и настройки остаются там же.
Все overview/alerts строятся только из текущего расчёта — история и timestamps
не синтезируются. Trust-gate действует во всех разделах: screening/default/
synthetic входы блокируют action recommendation. Во вкладке «План мастера»
доступны очередь задач, подробный checklist и выгрузка CSV; пустая очередь
является штатным результатом фильтрации, а не ошибкой.

**API:** тело — непустой JSON-массив объектов того же формата, что
`POST /api/v1/diagnose`:

```http
POST /api/v1/master-plan?plan_date=2026-08-23&min_risk=0.10&limit=10
Content-Type: application/json

[{"name":"Well-1","geometry":{"depth_m":3200,"tubing_id_m":0.062},
  "rate":{"q_oil_m3d":8,"q_water_m3d":72,"gor_m3m3":65},
  "fluid":{"gamma_oil":0.86,"gamma_gas":0.78,"salinity_ppm":290000},
  "thermal":{"t_surface_c":8,"geothermal_grad":0.033,"u_to":15,"production_days":400},
  "water":{"ions_mg_l":{"Na":95000,"Cl":205000,"Ca":28000,"HCO3":130},"ph":6,"t_c":40,"p_pa":5000000},
  "wax":{"wat_stock_tank_c":34,"wax_content_pct":6.5}}]
```

Сокращённый ответ:

```json
{"policy":{"mode":"partial","min_risk":0.1},"plan_date":"2026-08-23",
 "summary":{"task_count":1,"blocked_tasks":1},
 "tasks":[{"well":"Well-1","priority":4,"risk":0.4,"safe_to_act":false,
 "possible_oil_loss":{"central_m3d":3.2}}],"errors":[],
 "advisory_notice":"План предназначен для приоритизации…"}
```

Неверная дата и `min_risk` вне 0…1 дают 422; превышение batch limit — 413.
При partial success ошибки отдельных строк возвращаются в `errors`, а план
строится из успешных строк; если успешных строк нет, возвращается пустой план.

**Telegram:** `/plan` строит компактный план по последним успешным расчётам
текущего чата, `/plan_clear` очищает только историю этого чата. История
in-memory: максимум 20 расчётов на чат и 500 недавно активных чатов, без
межпроцессной синхронизации и постоянного хранилища; она исчезает при
перезапуске, не разделяется между несколькими worker-процессами. Расчёты
выполняются через `asyncio.to_thread`; сообщения режутся на части короче
Telegram-лимита 4096 символов.

### Прогноз осложнений во времени

`forecast_well` и `POST /api/v1/forecast` возвращают пять событий: АСПО, галит,
кальцит, коррозионный порог и снижение добычи. Статусы означают:

- `unavailable` — данных недостаточно для честного временного окна; дата и
  вероятность намеренно отсутствуют;
- `screening` — сценарное окно или качественная категория для предварительного
  скрининга, **не** точная вероятность и не гарантированная дата отказа;
- `calibrated` — статус разрешён только при holdout-validated артефакте с
  достаточным объёмом независимой выборки, приемлемым Brier score и совпадающим
  `dataset_id`. Вероятность появляется только если она явно записана в артефакте
  вместе с совпадающими `endpoints[mechanism]` и `probability_horizon_days`; эти
  metadata возвращаются в каждом событии рядом с evidence/calibration.

Без временной истории текущая тяжесть — это snapshot, а не часы до события,
поэтому часть дат недоступна. Для тренда нужны минимум четыре качественных
наблюдения, достаточный временной span и устойчивое направление. Будущие строки,
чужие скважины и будущие observed events отклоняются, чтобы исключить leakage;
одинаковые timestamps агрегируются медианой по метрике.

**CSV истории для сайта:**

```csv
well,timestamp,wax_severity,halite_severity,calcite_severity,corrosion_wall_loss_mm,oil_rate_m3_day,quality,source
Well-1,2026-07-14T12:00:00+00:00,0.20,0.10,0.15,0.05,100.0,good,measured
Well-1,2026-07-24T12:00:00+00:00,0.30,0.12,0.18,0.07,96.0,good,measured
```

`timestamp` — ISO 8601 с timezone; severity — 0…1; wall loss и дебит — конечные
неотрицательные числа; `quality`: `good|questionable|bad`; `source`:
`measured|derived|laboratory`. Необязательный `regime_id` маркирует единый
режим эксплуатации; старые CSV без столбца остаются совместимыми, а смешение
разных режимов блокирует трендовую экстраполяцию. После upload фонда или запуска demo откройте
вкладку «Прогноз во времени», выберите скважину и загрузите CSV истории в
боковой панели. Timeline показывает только события с реальным расчётным окном;
недатированные события остаются в отдельной таблице.

Для коррозионной даты нужен **резерв стенки**:
`current_wall_thickness_mm - minimum_allowable_wall_thickness_mm`, где обе
толщины измерены и первая больше минимально допустимой. Одна модельная скорость
коррозии не является оценкой остаточного ресурса и не учитывает локальный питтинг.

**API:**

```http
POST /api/v1/forecast
Content-Type: application/json

{"well":{"name":"Well-1","geometry":{"depth_m":3200,"tubing_id_m":0.062},
"rate":{"q_oil_m3d":100,"q_water_m3d":72,"gor_m3m3":65},
"fluid":{"gamma_oil":0.86,"gamma_gas":0.78,"salinity_ppm":290000},
"thermal":{"t_surface_c":8,"geothermal_grad":0.033,"u_to":15,"production_days":400},
"water":{"ions_mg_l":{"Na":95000,"Cl":205000,"Ca":28000,"HCO3":130},"ph":6,"t_c":40,"p_pa":5000000},
"wax":{"wat_stock_tank_c":34,"wax_content_pct":6.5}},
"as_of":"2026-08-23T12:00:00+00:00","history_snapshots":[],"observed_events":[]}
```

Сокращённый ответ:

```json
{"well":"Well-1","as_of":"2026-08-23T12:00:00Z",
 "summary":{"total":5,"unavailable":2,"screening":3,"calibrated":0},
 "events":[{"mechanism":"wax","status":"screening",
 "horizon_start_date":null,"horizon_end_date":null,"probability":null}],
 "advisory_notice":"Screening windows are scenarios, not guaranteed event dates or failure probabilities."}
```

**Telegram:** `/forecast` показывает прогноз для последнего успешного расчёта
этого чата, `/forecast Well-1` — для последнего точного совпадения имени после
Unicode/case/whitespace-нормализации. Это bounded in-memory история диагностик,
а не временной ряд измерений: она очищается при перезапуске и не создаёт дат или
вероятностей. Сначала выполните `/aspo` или пошаговый расчёт.

> Ограничение и disclaimer: прогноз предназначен для screening и поддержки
> инженерного решения. Он не является сертификатом остаточного ресурса,
> автоматическим управлением, гарантией даты отказа или заменой промысловых
> исследований, HSE-проверки и решения ответственного специалиста.

---

## 6. Какие данные нужны

Это главный раздел. Ниже — **всё**, что нужно ввести, с единицами,
типичными значениями и указанием, откуда это берётся.

### Минимальный пример кода

```python
from galit import (
    WellCase, WellGeometry, ProductionRate, FluidProperties,
    ThermalParams, WaterAnalysis, WaxProperties, diagnose,
)

case = WellCase(
    name="Речицкая 123",
    geometry=WellGeometry(depth_m=3200.0, tubing_id_m=0.062,
                          inclination_deg=15.0),
    rate=ProductionRate(q_oil_m3d=8.0, q_water_m3d=72.0, gor_m3m3=65.0),
    fluid=FluidProperties(gamma_oil=0.86, gamma_gas=0.78,
                          salinity_ppm=290_000.0),
    thermal=ThermalParams(t_surface_c=8.0, geothermal_grad=0.033,
                          u_to=15.0, production_days=400.0),
    water=WaterAnalysis(
        ions_mg_l={"Na": 95_000.0, "Cl": 205_000.0, "Ca": 28_000.0,
                   "Mg": 3_100.0, "K": 1_800.0,
                   "HCO3": 130.0, "SO4": 250.0},
        ph=6.0, t_c=40.0, p_pa=5e6,
    ),
    wax=WaxProperties(wat_stock_tank_c=34.0, wax_content_pct=6.5),
    co2_mol_frac=0.012,
    inhibitor_efficiency=0.0,
    lift_type="ЭЦН",
    p_wellhead_pa=1.4e6,
)

result = diagnose(case)
```

---

### 6.1. `WellGeometry` — конструкция скважины

| поле | единица | что это | где взять | типично |
|---|---|---|---|---|
| `depth_m` | **м** | глубина по стволу до забоя | паспорт скважины | 2000–4100 |
| `tubing_id_m` | **м** | **внутренний** диаметр НКТ | паспорт | 0.050 / 0.062 / 0.076 |
| `inclination_deg` | **град** | средний угол **от вертикали** | инклинометрия | 0–35 |
| `roughness_m` | **м** | шероховатость стали | по умолчанию | 4.6e-5 |

> ⚠️ **Диаметр — в метрах, не в миллиметрах.** НКТ 73 мм имеет
> внутренний диаметр ≈ 62 мм → `tubing_id_m=0.062`.
>
> ⚠️ **Угол от ВЕРТИКАЛИ.** Вертикальная скважина → `0`, а не `90`.

---

### 6.2. `ProductionRate` — режим работы

| поле | единица | что это | где взять |
|---|---|---|---|
| `q_oil_m3d` | **м³/сут** | дебит нефти | замер на устье, АГЗУ |
| `q_water_m3d` | **м³/сут** | дебит воды | замер на устье |
| `gor_m3m3` | **м³/м³** | газовый фактор | замер / промысловые данные |

Обводнённость считается автоматически: `q_water / (q_oil + q_water)`.

**Если дебит в тоннах/сут** — поделите на плотность:
нефть 8 т/сут при γ = 0,86 → `8 / 0.86 ≈ 9.3` м³/сут.

---

### 6.3. `FluidProperties` — свойства флюидов

| поле | единица | что это | типично |
|---|---|---|---|
| `gamma_oil` | — | отн. плотность нефти **по воде** | 0.83–0.89 |
| `gamma_gas` | — | отн. плотность газа **по воздуху** | 0.68–0.85 |
| `salinity_ppm` | **мг/л** | минерализация воды | 250 000–330 000 |
| `surface_tension` | **Н/м** | поверхностное натяжение | 0.03 |

**Если известна плотность в г/см³** — это и есть `gamma_oil`
(нефть 860 кг/м³ → `0.86`).

**Если известна плотность воды** — минерализацию можно оценить:
`salinity_ppm ≈ (ρ − 1.0) / 0.0007 × 1000`.
Для ρ = 1,20 г/см³ → ≈ 285 000 мг/л.

---

### 6.4. `ThermalParams` — теплофизика

| поле | единица | что это | типично |
|---|---|---|---|
| `t_surface_c` | **°C** | температура пород у поверхности | 8 |
| `geothermal_grad` | **К/м** | геотермический градиент | 0.028–0.038 |
| `u_to` | **Вт/(м²·К)** | коэф. теплопередачи к породе | 8–25 |
| `production_days` | **сут** | сколько работает непрерывно | 120–2500 |
| `k_earth` | Вт/(м·К) | теплопроводность породы | 2.5 |
| `alpha_earth` | м²/с | температуропроводность | 1.0e-6 |
| `r_to`, `r_wb` | м | радиусы НКТ / скважины | 0.038 / 0.108 |
| `cp_fluid` | Дж/(кг·К) | теплоёмкость флюида | 2100 |

> ⚠️ **Градиент в К/м, а не К/100 м.** 3,3 °C на 100 м → `0.033`.

**Проверка:** пластовая температура должна получиться разумной.
`t_surface_c + geothermal_grad × depth_m` = 8 + 0,033 × 3200 = **113,6 °C**.
Если получилось 300 — ошиблись в градиенте.

**`production_days` важен:** модель Ramey завышает температуру в раннем
переходном периоде. При работе меньше ~2 недель система выдаст
предупреждение (критерий Hagoort 2004: t_D < 100).

---

### 6.5. `WaterAnalysis` — химанализ пластовой воды

**Самое важное поле.** От него зависят оба солевых механизма.

```python
WaterAnalysis(
    ions_mg_l={
        "Na":   95_000.0,   # натрий
        "Cl":  205_000.0,   # хлорид
        "Ca":   28_000.0,   # кальций
        "Mg":    3_100.0,   # магний
        "K":     1_800.0,   # калий
        "HCO3":    130.0,   # гидрокарбонат  ← нужен для кальцита
        "SO4":     250.0,   # сульфат
    },
    ph=6.0,          # pH пробы
    t_c=40.0,        # температура отбора, C
    p_pa=5e6,        # давление отбора, Па
)
```

**Все концентрации — в мг/л (= ppm по массе).**

Поддерживаемые ионы:
`Na`, `K`, `Ca`, `Mg`, `Ba`, `Sr`, `Fe`, `Cl`, `SO4`, `HCO3`, `CO3`.

Неизвестный ион вызовет `KeyError` — это сделано намеренно, чтобы
опечатка не превратилась в тихо потерянный ион.

**Что обязательно:**

- `Na` и `Cl` — **без них галит не посчитается** (вернёт −99);
- `HCO3` — без него не посчитается кальцит (щёлочность = 0);
- `Ca` — нужен и для кальцита, и для эффекта одноимённого иона.

**Откуда брать:** стандартный протокол химанализа пластовой воды из
лаборатории НГДУ. Это рутинный анализ, он есть по любой скважине.

> 💡 **Почему нельзя ограничиться минерализацией.** Воды Припятского
> прогиба — хлоркальциевого типа: хлорид в **большом избытке** над
> натрием за счёт CaCl₂ и MgCl₂. Именно этот избыток высаливает галит
> (эффект одноимённого иона). Рост Ca²⁺ с 0 до 50 г/л при **неизменном**
> Na⁺ поднимает SI галита на **+0,28**. По одной цифре TDS это не видно.

---

### 6.6. `WaxProperties` — парафинистость

| поле | единица | что это | где взять |
|---|---|---|---|
| `wat_stock_tank_c` | **°C** | WAT дегазированной нефти при атм. давлении | лаборатория: ДСК или вискозиметрия |
| `wax_content_pct` | **% масс.** | содержание парафина | лабораторный анализ |
| `pour_point_c` | °C | температура застывания | опционально |

Типично для Припятского прогиба: WAT 22–48 °C, парафин 1,5–11 %.

> ⚠️ **Это единственный параметр, требующий лабораторного замера,
> которого может не быть.** Если WAT неизвестна — её оценивают по
> температуре застывания (WAT обычно на 10–20 °C выше) либо берут по
> аналогичным скважинам месторождения. Но тогда прогноз глубины АСПО
> становится оценочным, и это надо помнить.

---

### 6.7. Остальные поля `WellCase`

| поле | единица | что это | типично |
|---|---|---|---|
| `co2_mol_frac` | доля (0–1) | **мольная доля CO₂ в попутном газе** | 0.002–0.02 |
| `inhibitor_efficiency` | доля (0–1) | эффективность ингибитора коррозии | 0 или 0.85–0.9 |
| `lift_type` | строка | `"ЭЦН"` / `"ШГН"` / `"фонтан"` | — |
| `p_wellhead_pa` | **Па** | буферное давление | 0.8e6–2.2e6 |

> 🔴 **`co2_mol_frac` — самый критичный неизвестный параметр.**
> Скорость коррозии пропорциональна fCO₂^0,58. В открытых источниках
> данных по содержанию CO₂ в попутном газе Белоруснефти **найти не
> удалось**. По умолчанию в генераторе стоит 0,2–2 % как типичное для
> нефтяного попутного газа. **Это первое, что нужно уточнить** —
> завышенный CO₂ делает коррозию «победителем» в любом ранжировании.

**`inhibitor_efficiency`** ставьте `0`, если хотите увидеть **риск без
защиты** (что будет, если перестать подавать ингибитор). Ставьте `0.9`,
если моделируете текущее состояние скважины под защитой.

---

## 7. Как читать результат

```python
r = diagnose(case)
```

Объект `DiagnosisResult`:

| поле | тип | что внутри |
|---|---|---|
| `r.integrated_risk` | float | интегральный риск **0…1** |
| `r.dominant` | str | `"halite"` / `"calcite"` / `"wax"` / `"corrosion"` |
| `r.severity` | dict | тяжесть каждого механизма 0…1 |
| `r.wax_onset_m` | float / None | **глубина начала АСПО, м от устья** |
| `r.depths`, `r.temps`, `r.pressures` | list | профили по стволу |
| `r.wat_profile` | list | температура насыщения парафином |
| `r.scale` | dict | `si_halite`, `si_calcite`, `halite_kg_m3`, … |
| `r.corrosion` | dict | `rate_mm_yr`, `limiting`, `category`, … |
| `r.recommendation` | str | **что делать** |
| `r.warnings` | list | **предупреждения — читать обязательно** |

### Пример вывода

```
Скважина: Речицкое 746               интегральный риск: 0.517
Доминирующий механизм: коррозия

  Механизмы (0..1):
      коррозия  0.782  ###############################
         галит  0.549  #####################
          АСПО  0.409  ################
       кальцит  0.228  #########

  Глубина начала АСПО: 216 м

  SI(галит)   = +0.029   m = 6.63 при насыщении 6.13 моль/кг
  SI(кальцит) = +0.342   TDS = 398 г/л
  выпадение галита: 11.10 кг/м3 воды

  Коррозия: 0.291 мм/год, лимитирует массоперенос

  РЕКОМЕНДАЦИЯ: ингибиторная защита, целевая < 0.1 мм/год;
  лимитирует массоперенос -- рассмотреть снижение скорости потока;
  сопутствующие: галит, АСПО
  ! TDS 398 г/л -- LSI неприменим, используется Stiff-Davis
  ! Сужение сечения отложениями ускоряет коррозию: 0.291 -> 0.399 мм/год
```

### Как интерпретировать

**Индекс насыщения `SI`:**

| значение | смысл |
|---|---|
| `SI < −0.3` | недосыщение, риска нет |
| `SI ≈ 0` | равновесие, начало кристаллизации |
| `SI > +0.3` | сильное пересыщение, соль выпадает |

**Скорость коррозии** (шкала NACE):

| мм/год | категория |
|---|---|
| < 0.025 | низкая |
| 0.025–0.12 | умеренная |
| 0.12–0.25 | высокая |
| > 0.25 | очень высокая |

**`limiting`** — что тормозит коррозию:
- `"реакция"` → помогает ингибитор;
- `"массоперенос"` → ингибитор поможет хуже, **надо снижать скорость потока**.

**Semantics `wax_onset_m` и зоны АСПО:** глубина отсчитывается **от устья вниз**. Значение `500` означает, что холодная зона возможных отложений идёт от устья до 500 м (`0…500 м`), а не от 500 м до забоя. Если весь ствол холоднее WAT, onset равен глубине скважины и зона охватывает весь ствол. `wax_onset_m = None` означает, что весь ствол горячее WAT и отложений по модели нет. Это нормальный результат, а не ошибка.

---

## 8. Разбор фонда скважин

```python
from galit import rank_wells

cases = [case1, case2, case3, ...]      # список WellCase
ranked = rank_wells(cases)               # отсортировано по убыванию риска

for i, r in enumerate(ranked[:10], 1):
    print(f"{i}. {r.well}  риск={r.integrated_risk:.3f}  {r.dominant}")
```

`rank_wells` возвращает список `DiagnosisResult`, отсортированный по
`integrated_risk` **по убыванию**. Первые в списке — очередь на обработку.

### Загрузка своих данных

Сейчас фонд генерируется в [`galit/synthetic.py`](galit/synthetic.py).
Чтобы подключить реальные данные, **замените только этот модуль**
загрузчиком, который возвращает `list[WellCase]` — остальной код
менять не нужно:

```python
def load_fund(path: str) -> list[WellCase]:
    """Читает CSV/Excel и возвращает список WellCase."""
    ...
```

Это сознательное разделение: генератор синтетики и загрузчик реальных
данных взаимозаменяемы.

### Веса механизмов

```python
from galit.integrated import MECHANISM_WEIGHTS
# {"halite": 0.30, "calcite": 0.15, "wax": 0.30, "corrosion": 0.25}
```

> ⚠️ **Веса заданы экспертно и требуют калибровки** по реальной
> структуре отказов. Это первое, что делается при внедрении. Пока они
> отражают лишь предположение: коррозия и АСПО дают больше отказов,
> но галит при пересыщении вызывает мгновенное заклинивание.

---

## 9. Экономика пилота и break-even

Главный расчёт — не обещание годового эффекта, а проверяемая окупаемость
shadow-пилота. Стоимость пилота и ценность одной предотвращённой обработки,
одного отказа, суток простоя и сохранённой тонны задаются только как явные
`Assumption` с источником и confidence. Нулевой unit value допустим и означает,
что канал отдельно не окупает пилот; отрицательные и non-finite значения
отклоняются.

```python
from galit.economics import (
    Assumption, PilotOutcomeMix, PilotUnitEconomicsInput,
    compute_pilot_break_even, pilot_sensitivity,
)

# Заменить только на согласованные с заказчиком значения в одной валюте.
def a(name, value, unit):
    return Assumption(name, value, unit, "customer-approved input", "уточнить")

inputs = PilotUnitEconomicsInput(
    pilot_cost=a("Стоимость пилота", 0, "валюта"),
    treatment_value=a("Одна обработка", 0, "валюта/обработка"),
    failure_value=a("Один отказ", 0, "валюта/отказ"),
    downtime_day_value=a("Сутки простоя", 0, "валюта/сут"),
    saved_tonne_value=a("Сохранённая тонна", 0, "валюта/т"),
)
result = compute_pilot_break_even(inputs, PilotOutcomeMix())
```

Результат показывает отдельно treatments-only, failures-only,
downtime-days-only и saved-tonnes-only, затем смешанный сценарий и его
множитель до окупаемости. Compact sensitivity использует low/high каждого
допущения. Каналы нельзя складывать, пока заказчик не исключил двойной учёт
отказа, простоя и сохранённой добычи.

Старый расчёт `compute_effect(default_assumptions())` сохранён для
воспроизводимости как **scenario envelope, not forecast, not field validated**:
9,7–54,4 млн BYN/год с серединой 27,8 млн. Это не главный KPI и не обещание
эффекта: публичные ставки не заменяют себестоимость и маржу заказчика.
Чек-лист входов: [`reports/customer-input-checklist.md`](reports/customer-input-checklist.md).

---

## 10. Структура кода

```
galit/
  fluids.py       PVT: Standing, Dranchuk-Abou-Kassem, McCain,
                  Beggs-Robinson, Lee-Gonzalez-Eakin
  scale.py        галит (Potter 1977 + Питцер),
                  кальцит (Stiff-Davis; LSI — только для демонстрации ошибки)
  wellbore.py     T(z) по Ramey (1962), P(z) по Beggs-Brill (1973)
  wax.py          WAT(P), глубина начала АСПО, подбор технологии
  corrosion.py    de Waard-Milliams 1975 + resistance model 1995
  integrated.py   ← ЯДРО: качество/provenance, RiskPolicy, сценарии,
                  nodewise-коррозия, взаимодействие и ранжирование
  calibration/    строгая схема истории, split, калибровка и отчёты
  economics.py    эффект с прослеживаемыми допущениями
  synthetic.py    генератор синтетического фонда (НЕ данные заказчика)

tests/                  regression/API/dashboard/bot/calibration/release тесты
release_manifest.py     генерирует JSON + Markdown evidence с фактическим pytest
run_demo.ps1            Windows one-command preflight, evidence и demo
reports/                 машинно генерируемые release-manifest.json/.md
api.py                  FastAPI, OpenAPI: /docs и /openapi.json
dashboard.py            Streamlit-интерфейс CSV/XLSX
telegram_bot.py         aiogram-интерфейс /aspo
calibration_cli.py      offline calibration CLI
demo.py                 сквозная демонстрация (4 раздела + графики)
example_one_well.py     минимальный пример: своя скважина
```

---

## 11. Границы применимости — читать обязательно

### Оба стандартных индекса неприменимы к этим водам

Это не оговорка, а **центральный технический тезис проекта**:

| | пресная вода | рассол Припятского прогиба |
|---|---|---|
| TDS | 0,3 г/л | **292 г/л** |
| ионная сила | 0,01 моль/л | **5,77 моль/л** |
| LSI | −0,14 — применим | **+0,97 — ВНЕ ОБЛАСТИ** |
| Stiff–Davis | −0,17 — применим | **+0,27 — ВНЕ ОБЛАСТИ** |

- **LSI** ограничен 10 000 мг/л по ASTM D3739/D4582 — превышение **в 29 раз**
- **Stiff–Davis** калиброван до I = 4 моль/кг — мы за пределом

Расхождение между ними на реальном рассоле — **0,70 лог-единиц**, то есть
**впятеро** по концентрации. LSI систематически **завышает** склонность
к отложению.

**Практический вывод:** если где-то в расчётах используется LSI на
пластовой воде — прогноз смещён в сторону ложных тревог, и часть
обработок назначается зря.

Система в таких случаях **возвращает предупреждение, а не уверенное число**:

```python
from galit.scale import stiff_davis_index_checked, lsi_langelier

value, warnings = stiff_davis_index_checked(water)
for w in warnings:
    print(w)
```

### Что ещё нужно знать

1. **Расчёт галита — скрининговый**, не строгая модель Питцера.
   Погрешность ≈ ±0,15 лог-единиц при 25–90 °C. Для проектных решений
   нужна калибровка или ScaleSoftPitzer / OLI MSE.

2. **Градиенты WAT(P) — порядковые оценки, а не published коэффициенты.**
   Опубликованной корреляции WAT(P) не существует. В продуктиве их надо
   калибровать по лабораторным замерам (ДСК).

3. **Веса механизмов заданы экспертно** — требуют калибровки.

4. **Модель Ramey завышает T** в раннем переходном периоде
   (< ~2 недель работы). Система предупреждает.

### Четыре модели отвергнуты после сверки с первоисточниками

Это к вопросу о добросовестности расчёта:

1. **Oddo–Tomson не реализована сознательно.** Её коэффициент при
   давлении встречается в открытых источниках как `6.33e-3` и как
   `6.33e-5` — разница **в 100 раз** (при 3000 psi это 19 против 0,19
   лог-единиц). Кодировать формулу, которую нельзя проверить, нельзя.

2. Найденная формула растворимости галита давала **+4,1 % ошибки** при
   100 °C → заменена на Potter, Babcock & Brown (1977).

3. Распространённое утверждение «WAT максимальна вблизи P насыщения»
   **неверно** — она там **минимальна**. Модель исправлена, добавлен тест.

4. Фит USBR для константы Стиффа–Дэвиса **немонотонен** и завышен на
   +0,2…+0,6 при 40–70 °C. Дефекты **задокументированы в коде и выдаются
   пользователю**, а не замаскированы.

### Контрольные точки в тестах

- SI галита при насыщении = **0,0000** при 25/60/90 °C — термодинамический якорь
- канонический пример Beggs–Brill: **H_L(0) = 0,31**
- **температурный максимум de Waard**: коррозия растёт до ~70 °C, затем
  падает из-за плёнки FeCO₃
- эффект одноимённого иона: Ca²⁺ 0 → 50 г/л поднимает SI галита на **+0,28**
- профиль Ramey: на забое T = пластовой, поток везде теплее пород

---

## 12. Типичные ошибки при использовании

| ошибка | симптом | как исправить |
|---|---|---|
| диаметр НКТ в мм | абсурдная скорость потока, коррозия 0 или огромная | `tubing_id_m=0.062`, не `62` |
| градиент в К/100 м | пластовая T под 1000 °C | `0.033`, не `3.3` |
| угол от горизонтали | неверный градиент давления | вертикаль = `0` |
| дебит в т/сут | заниженные скорости | поделить на плотность |
| забыт `HCO3` | `si_calcite` странно низкий | добавить в химанализ |
| забыт `Na` или `Cl` | `si_halite = -99` | добавить в химанализ |
| `co2_mol_frac` в % | коррозия в разы завышена | `0.012`, не `1.2` |
| давление в МПа | всё сломано | `p_pa=5e6`, не `5` |

**Быстрая самопроверка после ввода данных:**

```python
r = diagnose(case)
print(f"Пластовая T: {r.temps[-1]:.0f} C")      # ожидаем 60–120
print(f"Забойное P:  {r.pressures[-1]/1e6:.0f} МПа")  # ожидаем 20–45
print(f"TDS:         {case.water.tds_mg_l/1000:.0f} г/л")  # ожидаем 250–400
```

Если что-то из этого выглядит дико — ошибка в единицах, а не в модели.

---

## 14. Baseline-сравнение и измеримый пилот

Модуль `galit/evaluation/pilot.py` сравнивает три заранее зафиксированные стратегии: календарный график, порог независимых механизмов и интегральное ранжирование GALIT. Оценка требует фактический `event_outcome`; supplied CSV без target получает `status=blocked`, потому что одни scores не доказывают качество.

Основная метрика пилота — NDCG@K на untouched holdout; дополнительно считаются Precision@K, Recall@K, пропущенные события и лишние вмешательства. Граница ties обрабатывается как ожидаемое значение по всем перестановкам tied group. Split обязан иметь непересекающиеся wells и строгую хронологию `train < calibration < holdout`.

- Data contract: [`reports/pilot-data-contract.md`](reports/pilot-data-contract.md)
- Pre-registered shadow protocol на 20–30 скважин: [`reports/pre-registered-pilot-protocol.md`](reports/pre-registered-pilot-protocol.md)
- Dashboard: вкладка **«Сравнение с baseline / Пилот»**, отдельно от production recommendation; там доступны XLSX-шаблон outcomes, blocked status, labels, assumptions и split summary.

Demo допускает только маркировку **SYNTHETIC / ILLUSTRATIVE / NOT FIELD VALIDATED**; это illustrative comparison, не accuracy. Prevented loss/net value появляются только при явно переданных unit costs и prevention assumptions и остаются сценарным, не причинным расчётом.

---

## 13. Источники моделей

| модель | источник |
|---|---|
| растворимость галита | Potter, Babcock & Brown, *J. Research USGS* 5(3) 389, **1977** |
| коэффициенты активности | Pitzer & Mayorga, **1973** |
| Stiff–Davis | *Pet. Trans. AIME* 195, 213, **1952**; фит USBR по ASTM D4582 |
| Langelier (LSI) | ASTM D3739 — реализован **только** для демонстрации ошибки |
| CO₂-коррозия | de Waard & Milliams **1975**; de Waard, Lotz, Dugstad, *CORROSION/95* Paper 128 |
| температура в стволе | Ramey, *JPT* 14(4) 427, SPE-96-PA, **1962** |
| ограничение Ramey | Hagoort, *SPE J* 9(4) 465, **2004** |
| градиент давления | Beggs & Brill, *JPT* 25(5) 607, **1973** |
| PVT нефти | Standing **1947**; Beggs & Robinson **1975** |
| z-фактор газа | Dranchuk & Abou-Kassem, **1975** |
| вязкость газа | Lee, Gonzalez & Eakin |
| свойства рассола | McCain, **1991** |

---

## 14. Качество данных, сценарии и калибровка

### Provenance и `production_mode`

`DataProvenance` хранит источник каждого критичного поля: `measured`, `derived`, `default` или `synthetic`. `assess_quality()` возвращает полноту, класс A–D, причины и `production_ready`. Обычный `diagnose(case)` допускает screening и явно пишет предупреждение; `diagnose(case, production_mode=True)` отклоняет расчёт (`DataQualityError`), если хотя бы в критичной группе есть default/synthetic/missing значения. Dashboard имеет переключатель промышленного режима, API — query-параметр `production_mode=true`. Для промышленного решения нужны фактические данные по стволу/режиму, воде, WAT/парафину и CO₂/ингибитору.

### `RiskPolicy` и сценарные интервалы

`RiskPolicy` версионирует `policy_id`, `version`, четыре неотрицательных веса (сумма 1) и пороги warn/critical. Базовые веса — 0.30/0.15/0.30/0.25 для halite/calcite/wax/corrosion; это экспертный baseline, не результат промысловой калибровки. `UncertaintyConfig(seed=..., samples>=20)` включает воспроизводимый sensitivity ensemble. Результат содержит scenario-интервалы p05/p50/p95 для интегрального риска, механизмов и onset, а также вероятность отложений. Это **сценарные интервалы, не статистические доверительные интервалы**. В API: `include_uncertainty=true`, `uncertainty_seed`, `uncertainty_samples`.

### Исправленные физические semantics

- Кальцит считает screening-эффект сброса давления/дегазации CO₂: pH у устья не должен искусственно уменьшаться. Это ограниченная инженерная оценка, не полный карбонатный flash/speciation.
- Коррозия считается nodewise на согласованных узлах `depths/T/P/PVT`; результат выбирает максимум и `depth_of_max_m`. При сужении весь профиль пересчитывается повторно, а не только одна условная точка.
- Галит остаётся screening-моделью активности с оценочной погрешностью; для проектирования нужна строгая электролитная модель и реальные данные.

### Calibration artifact и runtime

`calibration_cli.py calibrate` пишет строгий JSON artifact schema `2.0`: fingerprint/split,
поддерживаемые параметры, risk-policy, train/holdout metrics, limitations и
`validation_status`. Runtime применяет только `thermal.u_to` или четыре веса риска,
локально для вызова `diagnose`/`rank_wells`, без изменения глобального состояния.
Artifacts без `schema_version` нужно пересоздать CLI; неявной небезопасной миграции нет.
`blocked` и artifacts с NaN/inf, неизвестными параметрами, несовместимой версией или
ненормированными весами отклоняются. По умолчанию API и dashboard используют baseline.
API может загрузить только operator-selected startup artifact из allow-list root через
`GALIT_CALIBRATION_ARTIFACT` и `GALIT_CALIBRATION_ROOT`; request paths не принимаются.
Только статус `holdout-validated` означает наличие подходящих holdout targets; software
tests и synthetic calibration не являются field validation.

### Точная схема calibration history

Создайте CSV/XLSX командой ниже — это надёжнее ручного набора заголовков. Обязательные столбцы snapshot schema 1.0:

```text
schema_version, well_id, timestamp, source, quality,
depth_m, tubing_id_m, inclination_deg, roughness_m,
q_oil_m3d, q_water_m3d, gor_m3m3, gamma_oil, gamma_gas,
salinity_ppm, surface_tension_n_m, t_surface_c,
geothermal_grad_k_m, k_earth_w_mk, alpha_earth_m2_s,
u_to_w_m2k, r_to_m, r_wb_m, cp_fluid_j_kgk, production_days,
na_mg_l, cl_mg_l, ca_mg_l, mg_mg_l, k_mg_l, hco3_mg_l, so4_mg_l,
ph, water_t_c, water_p_pa, wat_stock_tank_c, wax_content_pct,
co2_mol_frac, inhibitor_efficiency, lift_type, p_wellhead_pa
```

Опциональные targets/metadata:

```text
target_temperature_c, target_pressure_pa, measurement_depth_m,
target_wax_onset_m, target_corrosion_mm_y, event_label, risk_label,
is_synthetic
```

`timestamp` обязан содержать timezone; `source` = `measured|derived|laboratory`, `quality` = `good|questionable|bad`. Единицы зашиты в имена полей. Строка — самостоятельный snapshot одной скважины в один момент времени; пары `(well_id,timestamp)` уникальны и упорядочены. Targets не используются как features (проверка leakage). XLSX использует лист `snapshots`; заготовлены также листы `observations`, `events`, `treatments`.

### Что прислать завтра

Лучше прислать заполненный XLSX, созданный первой командой, **без переименования колонок**. Минимально нужны несколько скважин и, по возможности, несколько дат на скважину; фактические входы из обязательной схемы; глубина замера `measurement_depth_m`; измеренные температура/давление; лабораторный WAT и парафин; полный ионный анализ с pH/T/P пробы; CO₂; режим и эффективность ингибирования; измеренная коррозия; фактический onset/интервал АСПО; события осложнений/обработок и `risk_label` по согласованной шкале. Укажите источник и качество каждой строки. Не заменяйте отсутствующие факты нулями: сначала согласуйте пропуск.

```powershell
# 1) шаблон для завтрашней таблицы
.\.venv\Scripts\python.exe calibration_cli.py generate-template tomorrow-history.xlsx
# пример отдельным файлом, только для понимания формата
.\.venv\Scripts\python.exe calibration_cli.py generate-template synthetic-example.xlsx --example

# 2) после заполнения
.\.venv\Scripts\python.exe calibration_cli.py validate tomorrow-history.xlsx

# 3) физическая калибровка thermal.u_to и независимая оценка
.\.venv\Scripts\python.exe calibration_cli.py calibrate tomorrow-history.xlsx physical-params.json --kind physical --split group --method robust --seed 0
.\.venv\Scripts\python.exe calibration_cli.py evaluate tomorrow-history.xlsx physical-params.json --json physical-report.json --markdown physical-report.md

# 4) отдельно — калибровка весов RiskPolicy по risk_label
.\.venv\Scripts\python.exe calibration_cli.py calibrate tomorrow-history.xlsx risk-policy.json --kind risk-policy --split group --seed 0
```

Физические параметры и risk-policy калибруются раздельно; split делается по скважинам (`group`) либо временным group holdout, без пересечения скважин. Synthetic smoke подтверждает только работоспособность pipeline и **не является свидетельством точности**.

> **Честный статус:** ГАЛИТ не промышленно откалиброван до реальных данных. До получения и независимой проверки промысловой истории результаты являются screening/decision-support, а не основанием для автономного технологического решения.

### Реальные оставшиеся ограничения

1. Нет промысловой калибровки и внешней валидации; baseline-веса экспертные.
2. Кальцит — screening дегазации + Stiff–Davis вне диапазона на сверхкрепких рассолах; не полный flash/Pitzer speciation.
3. Галит — приближённая модель; WAT(P), теплообмен и blockage требуют site calibration.
4. Коррозия — 1D steady screening без локальной геометрии, песка, H₂S, кислорода и истории плёнки/дозирования.
5. Scenario p05/p50/p95 отражают заданные диапазоны чувствительности, а не доказанную вероятностную модель.
6. Dashboard defaults и типовая вода бота пригодны только для screening; `production_mode` требует фактических критичных полей.

---

## Воспроизведение

Одна команда на Windows проверяет Python, запускает полный `pytest`, формирует
`reports/release-manifest.json` и `reports/release-manifest.md`, затем выполняет
детерминированное синтетическое demo:

```powershell
.\run_demo.ps1
```

Manifest фиксирует версию, UTC timestamp, Git commit/dirty state, Python,
фактический результат и число тестов, seed и ключевые показатели demo. Метки
`synthetic`, `scenario` и `not validated` обязательны: прохождение тестов — это
**software verification, не model validation** на независимых промысловых данных.

Отдельные команды:

```powershell
.\.venv\Scripts\python.exe release_manifest.py
.\.venv\Scripts\python.exe -m compileall -q galit api.py dashboard.py telegram_bot.py calibration_cli.py release_manifest.py
.\.venv\Scripts\python.exe demo.py --plots
.\.venv\Scripts\python.exe example_one_well.py

# Интерфейсы (каждая команда запускается отдельно; Ctrl+C для остановки)
.\.venv\Scripts\python.exe -m streamlit run dashboard.py
.\.venv\Scripts\python.exe -m uvicorn api:app --host 127.0.0.1 --port 8000
Copy-Item .env.example .env
# Откройте .env и укажите GALIT_BOT_TOKEN; файл .env не попадёт в Git.
.\.venv\Scripts\python.exe telegram_bot.py
```

API: `GET /api/v1/health`, `POST /api/v1/diagnose`; интерактивная схема — `http://127.0.0.1:8000/docs`. Бот требует сеть только при фактическом запуске polling; импорт, parsing и расчёт тестируются offline.
